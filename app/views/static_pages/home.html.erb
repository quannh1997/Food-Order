
    <div class="container">
      <div class="row">
<div class="col-4">
      <div class="row"> </div>
        </div>
      </div>
    </div>
    <hr>
    <h2 class="text-center">RECOMMENDED RESTAURANT</h2>
    <hr>
    <div class="container">
      <div class="row text-center">
        <!--each restaurant-->
        <% @restaurants = Restaurant.all %>      
        <% @restaurants.each do |restaurant| %>
        <div class="col-md-4 pb-1 pb-md-0" style="margin-bottom: 20px;">
          <div class="card">
            <img class="card-img-top" src="<%= restaurant.image %>" alt="Card image cap" style="width: 338px; height: 225px">
            <div class="card-body">
              <h5 class="card-title"><%= restaurant.name %></h5>
              <p class="card-text">Rate : <%= restaurant.average_star %>*</p>
              <!--<a href="/EON51" class="btn btn-primary">Choose</a>-->
              <a href="<%= restaurant_path(restaurant) %>" class="btn btn-primary">Choose</a>
            </div>
          </div>
        </div>
        <% end %>
        <!--each restaurant-->

        
        
      </div>
    </div>
    <hr>
